/*class Planet {
	
	constructor(x, y, r) {
		this.x = x;
		this.y = y;
		this.r = r;
		createPlanet();
	}
 	
 	createPlanet() {
		var _space = document.getElementById("space");
		var space= canvas.getContext("2d");
		ctx.beginPath();
		ctx.arc(95,50,40,0,2*Math.PI);
		ctx.stroke();
	}
}*/